from setuptools import setup, find_packages

setup(name = "SiDaTa",
      version = "0.1",
      description = "The Simple Data Table package.",
      author = "William Max Alexander",
      packages = find_packages())